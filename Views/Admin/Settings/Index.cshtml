@model Dictionary<string, string>
@{
    ViewData["Title"] = "Cấu Hình Hệ Thống";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    string GetVal(string key, string def = "") => Model.ContainsKey(key) ? Model[key] : def;
}

<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom border-secondary-subtle">
    <h1 class="h2 text-warning fw-medium">Cấu Hình Hệ Thống</h1>
</div>

<div class="row">
    <div class="col-md-8">
        <div class="card bg-body border border-secondary-subtle shadow-sm">
            <div class="card-body p-4">
                <form method="post" action="/admin/settings/save" enctype="multipart/form-data">
                    @Html.AntiForgeryToken()

                    <h5 class="text-info fw-medium mb-3 border-bottom border-secondary-subtle pb-2">Thông tin chung</h5>
                    <div class="mb-3">
                        <label class="form-label text-body-emphasis fw-medium">Tên Website</label>
                        <input type="text" name="site_name" value="@GetVal("site_name", "CinemaX")" class="form-control bg-light text-body-emphasis border-secondary-subtle">
                    </div>
                    <div class="mb-3">
                        <label class="form-label text-body-emphasis fw-medium">Email liên hệ</label>
                        <input type="email" name="contact_email" value="@GetVal("contact_email")" class="form-control bg-light text-body-emphasis border-secondary-subtle">
                    </div>
                    <div class="mb-3">
                        <label class="form-label text-body-emphasis fw-medium">Số điện thoại</label>
                        <input type="text" name="contact_phone" value="@GetVal("contact_phone")" class="form-control bg-light text-body-emphasis border-secondary-subtle">
                    </div>
                    <div class="mb-4">
                        <label class="form-label text-body-emphasis fw-medium">Địa chỉ trụ sở</label>
                        <input type="text" name="address" value="@GetVal("address")" class="form-control bg-light text-body-emphasis border-secondary-subtle">
                    </div>

                    <h5 class="text-info fw-medium mb-3 border-bottom border-secondary-subtle pb-2">Logo & Giao diện</h5>
                    <div class="mb-4">
                        <label class="form-label text-body-emphasis fw-medium">Logo trang web</label>
                        @if (!string.IsNullOrEmpty(GetVal("site_logo")))
                        {
                            <div class="mb-2">
                                <img src="@GetVal("site_logo")" alt="Logo" class="bg-light p-2 rounded border border-secondary-subtle" style="max-height: 50px;">
                            </div>
                        }
                        <input type="file" name="site_logo" class="form-control bg-light text-body-emphasis border-secondary-subtle" accept="image/*">
                    </div>

                    <h5 class="text-info fw-medium mb-3 border-bottom border-secondary-subtle pb-2">Mạng xã hội</h5>
                    <div class="mb-3">
                        <label class="form-label text-body-emphasis fw-medium">Facebook URL</label>
                        <input type="url" name="facebook_url" value="@GetVal("facebook_url")" class="form-control bg-light text-body-emphasis border-secondary-subtle">
                    </div>
                    <div class="mb-3">
                        <label class="form-label text-body-emphasis fw-medium">Instagram URL</label>
                        <input type="url" name="instagram_url" value="@GetVal("instagram_url")" class="form-control bg-light text-body-emphasis border-secondary-subtle">
                    </div>
                    <div class="mb-4">
                        <label class="form-label text-body-emphasis fw-medium">YouTube URL</label>
                        <input type="url" name="youtube_url" value="@GetVal("youtube_url")" class="form-control bg-light text-body-emphasis border-secondary-subtle">
                    </div>

                    <div class="text-end border-top border-secondary-subtle pt-3">
                        <button type="submit" class="btn btn-primary fw-medium px-4">Lưu cấu hình</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
